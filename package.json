{
  "name": "expressbasic",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.ts",
  "scripts": {
    "build:prod": "npm install && npx tsc",
    "start:prod": "node dist/index.js",
    "start": "npx tsc && cp -r ./public ./dist/public/ && npx tsx src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
